interface:
  display_name: "Agent Access"
  short_description: "Universal access layer for agent-native CLIs, browser fallback, and contribution"
  default_prompt: "Use Agent Access as a thin universal access layer: prefer existing agent-native CLIs and structured retrieval, then use browser/CDP or Computer Use only for CLI/API gaps."
